// PostCSS config for the Tailwind v3 page build (used by the Tailwind CLI and,
// if you wire it in, by Vite). The island (sources/) does not depend on this.
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
